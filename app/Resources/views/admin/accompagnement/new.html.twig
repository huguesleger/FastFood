{% extends ':admin:templateAdmin.html.twig' %}

  {% block content %}
       
    <h1>Accompagnement creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('accompagnement_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}

{% block scriptForm %}
    <script type="text/javascript" src="{{asset('js/form.js')}}"></script>
{% endblock %}
